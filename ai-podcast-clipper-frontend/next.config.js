/**
 * Next.js Configuration
 * =====================
 *
 * Configuration file for AI Podcast Clipper Next.js application.
 * Includes environment validation and build settings.
 *
 * Run `build` or `dev` with `SKIP_ENV_VALIDATION` to skip env validation.
 * This is especially useful for Docker builds.
 *
 * Author: Deepak Singhal
 */
import "./src/env.js";

/** @type {import("next").NextConfig} */
const config = {
  // Add any custom Next.js configuration here
  // e.g., experimental features, redirects, etc.
};

export default config;
